import { makeRunJavascriptComponent } from "./support.js";
import type { Inputs, Outputs } from "../js-components/context-to-title.js";

/**
 * This function was generated by @breadboard-ai/build-code from
 * src/js-components/context-to-title.ts
 */
export const contextToTitle = makeRunJavascriptComponent<Inputs, Outputs>({
  code: `"use strict";
// ../types/dist/src/index.js
/**
 * @license
 * Copyright 2024 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */

// src/js-components/context-to-title.ts
/**
 * @license
 * Copyright 2024 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function run(inputs) {
  let context = inputs.context;
  const result = {
    body: {
      title: "Untitled Slide Deck"
    }
  };
  if (typeof context === "string") {
    result.body.title = context;
    return result;
  }
  if (!Array.isArray(context)) {
    context = [context];
  }
  const last = context.at(-1);
  if (!last || !last.parts || !last.parts.length) {
    return result;
  }
  result.body.title = last.parts.map((part) => "text" in part ? part.text : "").join(" ");
  return result;
}
`,
  inputSchema: {
    type: "object",
    properties: { context: { $ref: "#/definitions/def-0" } },
    required: ["context"],
    definitions: {
      "def-0": {
        description:
          "All Breadboard values must be JSON serializable, and this is the set of\nJSON serializable types.",
        anyOf: [
          { type: "array", items: { $ref: "#/definitions/def-0" } },
          {
            type: "object",
            additionalProperties: { $ref: "#/definitions/def-0" },
          },
          { type: ["null", "string", "number", "boolean"] },
        ],
      },
    },
  },
  outputSchema: {
    type: "object",
    properties: {
      body: {
        type: "object",
        properties: { title: { type: "string" } },
        required: ["title"],
      },
    },
    required: ["body"],
  },
});
